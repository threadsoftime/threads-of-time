[package]
name = "tot-harness-client"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
axum = "0.7"
